--- a/mt_wifi/common/cmm_sanity.c
+++ b/mt_wifi/common/cmm_sanity.c
@@ -2266,6 +2266,7 @@ BOOLEAN PeerProbeReqSanity(
 	IN ULONG MsgLen,
 	OUT PEER_PROBE_REQ_PARAM * ProbeReqParam)
 {
+	UCHAR		*ssid_ie = NULL;
 	PFRAME_802_11 Fr = (PFRAME_802_11)Msg;
 	UCHAR		*Ptr;
 	UCHAR		eid = 0, eid_len = 0, *eid_data;
@@ -2316,11 +2317,28 @@ BOOLEAN PeerProbeReqSanity(
 	COPY_MAC_ADDR(ProbeReqParam->Addr1, &Fr->Hdr.Addr1);
 #endif
 
-	if (Fr->Octet[0] != IE_SSID || Fr->Octet[1] > MAX_LEN_OF_SSID || Fr->Octet[1] > (MsgLen - LENGTH_802_11 - 2)) {
+	Ptr = Fr->Octet;
+	total_ie_len = MsgLen - LENGTH_802_11;
+	while (total_ie_len >= 2) {
+		UINT ie_len = Ptr[1] + 2;
+
+		if (ie_len > total_ie_len)
+			break;
+		if (Ptr[0] == IE_SSID) {
+			ssid_ie = Ptr;
+			break;
+		}
+
+		Ptr += ie_len;
+		total_ie_len -= ie_len;
+	}
+
+	if (!ssid_ie || ssid_ie[1] > MAX_LEN_OF_SSID) {
 		MTWF_DBG(NULL, DBG_CAT_TEST, CATTEST_SANITY, DBG_LVL_INFO,
 			"sanity fail - wrong SSID IE\n");
 		return FALSE;
 	}
+	total_ie_len = 0;
 #ifdef EM_PLUS_SUPPORT
 	if (IS_BROADCAST_MAC_ADDR(ProbeReqParam->Addr1)) {
 		int apidx;
@@ -2373,8 +2391,8 @@ BOOLEAN PeerProbeReqSanity(
 	 * call that in certification case. Correct apidex passed in below API.
 	 */
 
-	ProbeReqParam->SsidLen = Fr->Octet[1];
-	NdisMoveMemory(ProbeReqParam->Ssid, &Fr->Octet[2], ProbeReqParam->SsidLen);
+	ProbeReqParam->SsidLen = ssid_ie[1];
+	NdisMoveMemory(ProbeReqParam->Ssid, ssid_ie + 2, ProbeReqParam->SsidLen);
 #ifdef CONFIG_AP_SUPPORT
 	COPY_MAC_ADDR(Addr1, &Fr->Hdr.Addr1);
 #ifdef WSC_AP_SUPPORT
--- a/mt_wifi/common/fsm/fsm_sync.c
+++ b/mt_wifi/common/fsm/fsm_sync.c
@@ -2313,7 +2313,7 @@ static VOID ApPeerProbeReqAction(RTMP_ADAPTER *pAd, MLME_QUEUE_ELEM *Elem)
 
 	NdisZeroMemory(ProbeReqParam, sizeof(PEER_PROBE_REQ_PARAM));
 	if (PeerProbeReqSanity(pAd, Elem->Msg, Elem->MsgLen, ProbeReqParam) == FALSE) {
-		MTWF_DBG(pAd, DBG_CAT_MLME, CATMLME_SYNC, DBG_LVL_ERROR,
+		MTWF_DBG(pAd, DBG_CAT_MLME, CATMLME_SYNC, DBG_LVL_WARN,
 			"PeerProbeReqSanity failed!\n");
 		os_free_mem(ProbeReqParam);
 		return;
--- a/mt_wifi/os/linux/cfg80211/cfg80211_rx.c
+++ b/mt_wifi/os/linux/cfg80211/cfg80211_rx.c
@@ -232,7 +232,7 @@ BOOLEAN CFG80211_HandleP2pProbeReq(PRTMP_ADAPTER pAd, struct wifi_dev *wdev,
 	{
 		if (PeerProbeReqSanity(pAd, (VOID *)pRxBlk->pData,
 			pRxBlk->DataSize, ProbeReqParam) == FALSE) {
-			MTWF_DBG(pAd, DBG_CAT_RX, CATRX_MGMT, DBG_LVL_ERROR,
+			MTWF_DBG(pAd, DBG_CAT_RX, CATRX_MGMT, DBG_LVL_WARN,
 				"PeerProbeReqSanity fail!\n");
 			os_free_mem(ProbeReqParam);
 			return TRUE; /*drop when SSID sanity check fails in ProbeReq */
